n = int(input("Enter first no."))
m = int(input("Enter second no."))
def multiply(a, b):
     print("The product is",a*b)
# The benefit of a function is that we do not have to write a code again
# and again. We can just call the function whenever we require the same
# operation to take place.
multiply(n, m)
o = int(input("Enter first no."))
p = int(input("Enter second no."))
multiply(o, p)




